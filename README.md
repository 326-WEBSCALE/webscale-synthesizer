# Webscale Synthesizer

A web application based around a synthesizer for generating program constants.
The directories such as `project1` exist as individual submissions as part of the larger project.